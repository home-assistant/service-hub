[test]
coverageReporter = ["text", "lcov"]
coverageSkipTestFiles = true
coveragePathIgnorePatterns = ["test/**"]

# bun has no branch coverage; the vitest branches threshold (75%) is dropped.
[test.coverageThreshold]
statement = 0.85
function = 0.8
line = 0.85
