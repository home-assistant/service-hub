- call: issues.createComment
  params:
    issue_number: 21
    owner: contributor
    repo: core
    body: |-
      <!-- ha-bot-dashboard -->

      _Evaluating rules…_
- call: issues.updateComment
  params:
    owner: contributor
    repo: core
    comment_id: 1000
    body: |-
      <!-- ha-bot-dashboard -->

      👋 Hi! Thanks for contributing to **Home Assistant**.

      This is your PR dashboard which flags anything you need to address before your PR can be reviewed. Once everything is green, you can press the **'Ready for review'** button at the bottom of the page to notify reviewers to take a look.

      <details><summary>More information about this dashboard</summary>

      This dashboard automatically updates on every change to this PR and reevaluates the rules as you go. Until everything has been addressed, the PR stays in draft and you won't be able to put it into review.

      ### Skip a check that doesn't apply

      If you think a check doesn't apply to your PR (or the rule is bugged), comment `/ha-bot ignore "<check name>" "<reason>"` — the check name as shown in the table above. This will mark the check as ignored and let you put your PR in 'Ready for review'; `/ha-bot unignore "<check name>"` restores it.

      ### Bot commands

      Reply with `/ha-bot <command>` — several commands can be stacked, one per line:

      - `update` — Re-runs the bot's checks on the issue or pull request.
      - `unassign` — Removes the given integration domain's label and its code owners from the assignees. — e.g. `/ha-bot unassign "opensky"`
      - `ignore` — Waives a dashboard check: `ignore "<check name>" "<reason>"`. *(author)* — e.g. `/ha-bot ignore "Merge conflicts" "Broken rule, no merge conflicts present"`
      - `unignore` — Restores a waived dashboard check: `unignore "<check name>"`. *(author)* — e.g. `/ha-bot unignore "Merge conflicts"`
      - `close` — Closes the issue or pull request. *(code owners)*
      - `reopen` — Reopens the issue or pull request. *(code owners)*
      - `rename` — Renames the issue or pull request: `rename "<new title>"`. *(code owners)* — e.g. `/ha-bot rename "A more descriptive title"`
      - `add-label` — Adds a label (needs-more-information, problem in dependency, problem in custom component, problem in config, problem in device, feature-request) to the issue or pull request. *(code owners)* — e.g. `/ha-bot add-label "needs-more-information"`
      - `remove-label` — Removes a label (needs-more-information, problem in dependency, problem in custom component, problem in config, problem in device, feature-request) from the issue or pull request. *(code owners)* — e.g. `/ha-bot remove-label "needs-more-information"`
      - `mark-draft` — Marks the pull request as draft. *(code owners)*
      - `ready-for-review` — Removes the draft status from the pull request. *(code owners)*
      - `update-branch` — Updates the pull request branch with the base branch. *(code owners)*

      </details>

      ## Checks

      **✨ Everything's in order!**

      | Status | Check | Details |
      |--------|-------|---------|
      | :hourglass: | Code owner approval | Platinum integration — needs approval from a code owner before merging. |

      <details>
      <summary>1 check skipped</summary>

      | Status | Check | Details |
      |--------|-------|---------|
      | :heavy_minus_sign: | Review comments | No inline review comments yet. |

      </details>

      ---
      <sub>Last updated: 2026-06-15T12:00:00.000Z</sub>

      <!-- section:review-comments:{"id":"review-comments","title":"Review comments","status":"skip","message":"No inline review comments yet."} -->
      <!-- section:code-owner-approval:{"id":"code-owner-approval","title":"Code owner approval","status":"pending","message":"Platinum integration — needs approval from a code owner before merging."} -->
- call: repos.createCommitStatus
  params:
    owner: contributor
    repo: core
    sha: 8d8e8fa31782e096a8cc7762618c0d2edfb02649
    context: ha-bot
    state: failure
    description: 1 check pending
    target_url: https://github.com/contributor/core/issues/21#issuecomment-1000
